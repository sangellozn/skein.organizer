import { UserCollectionCreate } from './user-collection-create';

describe('UserCollectionCreate', () => {
  it('should create an instance', () => {
    expect(new UserCollectionCreate()).toBeTruthy();
  });
});
