import { SkeinPossessionUpdate } from './skein-possession-update';

describe('SkeinPossessionUpdate', () => {
  it('should create an instance', () => {
    expect(new SkeinPossessionUpdate()).toBeTruthy();
  });
});
