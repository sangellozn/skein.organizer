import { PrepareOrderResult } from './prepare-order-result';

describe('PrepareOrderResult', () => {
  it('should create an instance', () => {
    expect(new PrepareOrderResult()).toBeTruthy();
  });
});
